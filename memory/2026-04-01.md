# 2026-04-01 管家日誌

## 09:30 老闆回報 5 個主控版問題

### 問題與修正
1. **股票專案沒有自動更新** → 原因：整頁硬編碼 HTML，沒從 stock-data.json 動態讀取。修正：改為 loadStockData() 動態渲染
2. **外匯保證金新聞沒有自動更新** → 原因：JS 預期 `d.news.items` 但 JSON 是 `d.news` 陣列；socialDiscussions 格式也不對。修正：兼容兩種格式 + 社群討論專用渲染
3. **虛擬貨幣新聞沒有自動更新** → 同上
4. **Threads 發了沒放到已發佈** → 2026-03-31-002 已發佈（Thread ID: 17871167718595096）但 JSON 沒更新。修正：手動移到 published
5. **已發佈排序錯誤** → 修正：按 ID 倒序（新到舊）

### 額外修正
- **所有 `git add -A` 全面清除**：news_updater.js、publisher.js、social cron 全改為只 add 特定檔案
- **所有 git push 前加 `git pull --rebase`**
- **social-morning cron timeout**：從 120s 改為 300s
- **social cron 排序規則**：寫進 cron prompt，確保已發佈按新到舊排序
- **主控版新增**：外匯/虛擬貨幣綜合分析區（conclusion）、社群討論專用渲染

## 10:26 昨日遺留待辦處理

### 結果
1. ✅ **CLOSED_BY_BROKER 回查盈虧** — V6 已內建 `recover_broker_closed_pnl()`，trade_log 全部已回補完成，無遺漏
2. ✅ **15M 趨勢跟隨** — 這是 V5 舊策略，V6 已用全新 12 策略架構替代，舊策略不再執行
3. ✅ **深夜時段交易限制** — V6 已內建 ACTIVE_SESSIONS，外匯 UTC 7-21、美指 UTC 13-21、黃金 24H
4. ✅ **Google Drive 自動備份** — daily-backup cron 每天 06:00 正常運作，gog OAuth 已設定，4/1 備份 552K 上傳成功

### 教訓
- **HTML 硬編碼 = 不會自動更新**：所有主控版區塊都必須從 JSON 動態讀取
- **JSON 格式要前後端一致**：後端改了結構，前端 JS 也要同步改
- **git add -A 是定時炸彈**：永遠只 add 特定檔案

## 11:14 老闆回報社群+股票問題（第二輪修復）

### 問題
1. **社群已發佈貼文又跑回待審核** — cron 每次跑都用 write 整個覆寫 social-data.json，模型拼錯就蓋掉正確資料
2. **股票專案什麼都看不到** — stock-data.json 的 keyEvents 是字串不是陣列，JS forEach 炸掉整頁不渲染
3. **草稿用「最近」開頭** — cron prompt 沒更新禁止規則
4. **草稿內容不夠豐富**

### 根因分析
- 社群鬼打牆的根因：cron 讓模型自己拼整個 social-data.json → 每次跑都可能蓋掉正確資料 → 我手動改了也被下次 cron 覆蓋
- 應該第一次就追查 cron 覆蓋問題，不該只改 JSON 表面

### 修復
1. **建立 sync_dashboard.py** — 統一從 social/drafts/ 和 social/published/ 資料夾讀檔生成 JSON，不再讓模型拼
2. **更新 cron prompt** — 改成寫完草稿後跑 sync 腳本；加入「禁止最近開頭」規則
3. **修 stock keyEvents** — JS 改為兼容字串和陣列兩種格式
4. **重寫草稿** — 提問破題、加具體案例和數字
5. **2026-04-01-001 已發佈** — Thread ID: 17899096038264226

## 13:00 IG 輪播圖文大改版

### 老闆回饋
1. 上傳到雲端要建日期資料夾（不是全部丟在根目錄）
2. 需要附文案
3. 內容無聊、圖片不好看、排版不好看

### 修正
1. **generate_carousel.py v2**：漸層背景、裝飾幾何圖形、卡片式內容、進度圓點、更好的排版層次
2. **上傳流程**：upload_to_drive.sh 改為自動建 YYYY-MM-DD 日期資料夾
3. **新增文案檔**：每組輪播都附 caption txt
4. **重做內容**：「為什麼你的廣告一直在燒錢？」5 張，有具體案例和數字
5. **已上傳**：5 張圖 + 文案 → IG素材/2026-04-01/
6. **SKILL.md 更新**：Drive 資料夾結構改為日期制

### 嚴重教訓
- **修 bug 要追根因**：不是改 JSON 就好，要找出誰在覆寫 JSON
- **cron 讓模型拼 JSON = 定時炸彈**：用確定性腳本取代
- **浪費老闆 token**：來回修了三次才修好，老闆非常不滿。應該第一次就徹底查清楚
- **不要輸出處理過程**：老闆再次提醒回覆太囉嗦

## 22:00 每日總回報

### 外匯 (Capital.com)
- 餘額：$31,017.02
- 今日已平倉：8W/11L，淨利 +$146.25
  - US30 表現最佳：3W/0L，+$132.30
  - S&P 500：2W/0L，+$12.93
  - XAUUSD：3W/11L，+$1.02（大量小虧但整體微正）
- 持倉中：4 筆（US30 SELL、S&P500 SELL、NAS100 SELL、XAUUSD BUY）
- 觀察：XAUUSD 頻繁觸發止損，勝率低但單筆虧損有控制

### 虛擬貨幣模擬 (MAX)
- 資本：NT$27,613（起始 NT$28,674，累計 -3.70%）
- 今日：4W/2L，已實現 -NT$87.54
  - 贏的：ETH/TWD x3 (+NT$240.74)、BTC/USDT (+NT$83.62)
  - 虧的：DOGE/TWD (-NT$210.05)、SOL/TWD (-NT$201.85)
- 持倉中：ETH/USDT BUY @ 2131.03（持平）
- 問題：SOL 和 DOGE 頻繁止損，虧損幅度大於 ETH/BTC 的獲利

### 社群專案
- ✅ 今日兩篇 Threads 已發佈
  - 11:00 品牌思維（真人感 vs 精修素材）
  - 18:00 工具推薦（工具斷捨離）
- 連續發文第 5 天，累計 10 篇已發佈

### 技能專案
- 主控版 5 個問題全修完（動態渲染、JSON 格式統一、git add 安全化）
- IG 輪播圖文 v2（漸層背景、卡片式排版、Drive 日期資料夾）
- sync_dashboard.py 上線，社群 JSON 不再被 cron 覆寫

### 待觀察
- XAUUSD 高頻交易勝率偏低，需要檢討策略或提高信心門檻
- 虛擬貨幣 SOL/DOGE 表現不佳，可能需要收窄可交易幣種
